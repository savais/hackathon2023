export class SearchDef {
    constructor (
        public id: number,
        public name: string,
        public version: string,
        public type: string,
        public productFamily: string,
    ) {}
}